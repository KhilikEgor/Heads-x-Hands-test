package entities;

public class Monster extends Entity {
    public Monster(int hitPoints, int attack, int defense, int minDamage, int maxDamage) {
        super(hitPoints, attack, defense, minDamage, maxDamage);
    }
}
